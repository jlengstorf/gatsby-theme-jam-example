/** @jsx jsx */
import * as React from 'react'
import { jsx, Styled } from 'theme-ui'

export const LastedPosts = ({ children }) => {
  console.log('LastedPosts', children)

  return <>{children}</>
}
